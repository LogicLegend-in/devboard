services:
  devboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: logiclegend-devboard
    ports:
      - "8004:8004"
    environment:
      - PORT=8004
      - ENVIRONMENT=development
    restart: unless-stopped
